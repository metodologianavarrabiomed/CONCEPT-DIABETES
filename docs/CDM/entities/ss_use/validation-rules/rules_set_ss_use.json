{
    "description": "The expressions of the different rules follow the syntax of the SQL OLAP database \n        management system called DuckDB. For more information, visit https://duckdb.org/docs/sql/introduction. \n        Keep in mind that you only have to declare as a rule the statement that would follow a WHERE clause. \n        e.g.,  Select * from people WHERE {expression}\n        expression = age >= 18\n        ",
    "rules": [
        {
            "expression": "visit_type IN ('1', '2', '3', '4', '5', '6', '7', '8')",
            "name": "visit_type",
            "description": "Rule for column visit_type"
        },
        {
            "expression": "visit_loc IN ('1', '2', '3')",
            "name": "visit_loc",
            "description": "Rule for column visit_loc"
        },
        {
            "expression": "visit_derivation_PC IN ('0', '1')",
            "name": "visit_derivation_PC",
            "description": "Rule for column visit_derivation_PC"
        },
        {
            "expression": "consult_request IN ('0', '1')",
            "name": "consult_request",
            "description": "Rule for column consult_request"
        }
    ]
}